[package]
name = "safemask-wasm"
version = "0.1.0"
edition = "2021"

[lib]
# 🚀 必须声明为 cdylib，否则无法生成 .wasm 文件
crate-type = ["cdylib"]

[dependencies]
# WASM 核心桥梁
wasm-bindgen = "0.2"
js-sys = "0.3"

# 序列化（用于在 JS 和 Rust 之间传递复杂的规则对象）
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 🚀 保持算法库版本与桌面端一致
regex = "1.10"
aho-corasick = "1.1"
smallvec = { version = "1.11", features = ["serde"] }

# 辅助：查看 WASM 内部报错（可选，建议加上）
console_error_panic_hook = "0.1.7"